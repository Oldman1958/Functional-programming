""" 
Давайте напишем выражение-генератор, 
который будет генерировать кортеж состоящий из двух элементов: 
номера дня в году и названия дня недели.

За начало отсчета возьмем наш «любимый» 2022 год. 
Он начался в субботу, потом воскресенье, понедельник, вторник, ..., 
пятница, суббота и далее по кругу

Результат выражения-генератора сохраните в переменную days

Названия дней недели должны совпадать с этими значениями:

['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']
                  
Значит при первой итерации генератор должен вернуть кортеж

(1, 'Saturday')
                  
При второй итерации вернется значение

(2, 'Sunday')
                  
Ваша задача распечатать на удачу первые 77 дней 2022 года. 
Выводить на экран нужно сами кортежи 
и первые десять значений в выводе должны выглядеть вот так:

(1, 'Saturday')
(2, 'Sunday')
(3, 'Monday')
(4, 'Tuesday')
(5, 'Wednesday')
(6, 'Thursday')
(7, 'Friday')
(8, 'Saturday')
(9, 'Sunday')
(10, 'Monday')
"""

weekdays = ['Monday', 'Tuesday', 'Wednesday',
            'Thursday', 'Friday', 'Saturday', 'Sunday']

# 2022 год начался в субботу, значит день 1 соответствует индексу 5 в списке weekdays
days = (
    (day_num, weekdays[(5 + day_num - 1) % 7])
    for day_num in range(1, 366)
)

# Выводим первые 77 дней
for _ in range(77):
    print(next(days))
