"""
Функция aggregation - 2
Перепишите функцию aggregation так, чтобы она возвращала итоговое значение агрегации.

Функция aggregation по-прежнему должна принимать на вход функцию func и коллекцию элементов sequence.
"""


def aggregation(func, sequence):
    # Если последовательность пустая, возвращаем None (нет значения для агрегации)
    if not sequence:
        return None

    # Если в последовательности только один элемент, возвращаем его
    if len(sequence) == 1:
        return sequence[0]

    # Начальное значение — первый элемент последовательности
    current_value = sequence[0]

    # Проходим по оставшимся элементам последовательности (начиная со второго)
    for element in sequence[1:]:
        # Применяем функцию func к текущему накопленному значению и следующему элементу
        current_value = func(current_value, element)

    # Возвращаем итоговое агрегированное значение
    return current_value
